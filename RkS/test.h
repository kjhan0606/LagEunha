typedef struct AA{ 
	float y; 
	float z; 
	int imem; 
	int aa; 
	float x;
	float fmem; 
	indxflag u4if;
	char a,af,f;
} AA;


/*
#define xPos(gridinfo,p) ( (p)->x)
#define yPos(gridinfo,p) ( (p)->y)
#define zPos(gridinfo,p) ( (p)->z)
*/
